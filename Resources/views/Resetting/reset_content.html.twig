<form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset form-signin" role="form">
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-primary btn-block" type="submit">{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}</button>
</form>
